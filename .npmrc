# pnpm 11 runs a deps-status check before `pnpm run <script>` that exits non-zero
# when optional native build scripts (sharp, unrs-resolver, core-js-pure) are
# left unbuilt. Those aren't needed to run Storybook/dev, so don't let the check
# block script execution.
verify-deps-before-run=false
