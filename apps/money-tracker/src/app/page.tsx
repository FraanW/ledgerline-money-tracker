import { PersonaScrollLanding } from "../components/pages/PersonaScrollLanding";

/** Home `/` — the marketing scroll-landing (self-themed; shows all personas). */
export default function Home() {
  return <PersonaScrollLanding />;
}
