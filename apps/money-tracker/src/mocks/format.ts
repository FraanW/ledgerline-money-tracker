// One money helper for stories — the math is the package's, not reimplemented.
export { formatMoney, money, fromRupees, addMoney, subMoney, ZERO_INR } from "@ledgerline/types";
export type { Money, CurrencyCode } from "@ledgerline/types";
