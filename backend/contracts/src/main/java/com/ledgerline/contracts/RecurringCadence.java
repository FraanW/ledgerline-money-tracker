package com.ledgerline.contracts;

/** Mirror of the {@code recurring_cadence} enum (migration V11). */
public enum RecurringCadence {
    weekly,
    monthly,
    quarterly,
    yearly,
    irregular
}
